<%= form_with(model: gps_truck, local: true) do |form| %>
  <% if gps_truck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gps_truck.errors.count, "error") %> prohibited this gps_truck from being saved:</h2>

      <ul>
      <% gps_truck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
